public class ActiveItem : EquipmentItem
{
    // Start is called before the first frame update
    public ActiveItem(string name, string desc, string flavor, EquipmentType type, Effect effect) : base(name, desc, flavor, type, effect)
    {
    }
}
